* Bot-level permissions - some people can add commands, others not
* Channel-level permissions - "all mods can do this". Would tie in with the above.
* Channel currency, because why not.
* Quotes
* Better GUI (as a separate file)
* Per-channel echocommands?
* Recognizing channels where the bot is a mod?
